import axios from "axios";
import { SERVER } from "../Constants";

const api = axios.create({
    baseURL: SERVER
})

const API = {
    uploadBulkTests: async() => {
        try {
            
        } catch (error) {
            
        }
    },
    addCourse: async() => {
        try {
            
        } catch (error) {
            
        }
    },


}

export default API